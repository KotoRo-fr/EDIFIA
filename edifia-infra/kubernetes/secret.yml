# ============================================================
# EDIFIA — Secret (variables sensibles)
# ============================================================
# IMPORTANT: Les valeurs ci-dessous sont encodées en base64.
# Pour encoder: echo -n 'votre-valeur' | base64
# En production, utiliser un gestionnaire de secrets (Vault, Sealed Secrets, etc.)
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: edifia-secret
  namespace: edifia
  labels:
    app: edifia
    tier: config
type: Opaque
data:
  # edifia_secret (base64)
  DB_PASSWORD: ZWRpZmlhX3NlY3JldA==
  # change-me-in-production-jwt-secret
  JWT_SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tand0LXNlY3JldA==
  # Admin123!
  ADMIN_PASSWORD: QWRtaW4xMjMh
